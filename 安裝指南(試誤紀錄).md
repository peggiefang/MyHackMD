# 安裝指南（試誤紀錄）
[toc]
### :traffic_light: Hexo+GitHub 建立網站 (M1 MacBookAir Big Sur 11.3)
> [time=Mon, May 17, 2021 11:21 AM]

**前置：** 
* 下載 Xcode （並開啟、關閉）
* GitHub 建立 repository，user.github.io 。(我的user:peggiefang)

**依序安裝：**
1. node.js (https://nodejs.org)
2. `sudo npm install -g hexo-cli` (安裝 Command line interface for Hexo.)
3. sudo npm install
4. 確認以下皆安裝
	`node -v`
	`npm -v`
	`Git --version`
5. sudo npm install -g hexo (安裝 hexo)
6. 終端 cd 到想存放的目錄下
7. `hexo init` `blog` (hexo 初始化 local 端 網站文件夾 blog 名稱可自訂)
8. 開啟 剛剛建的 blog 資料夾
9. 開啟文件 `_config.yml`
10. 修改對應代碼，存檔關閉
```
deploy:
  type: git
  repository: https://github.com/peggiefang/peggiefang.github.io.git
  branch: master
```
* **type、repository、branch后均有空格**
11. 終端輸入 `hexo g`
12. 終端輸入 `hexo d`

* `hexo g` error 則輸入`npm install hexo --save`，並在執行一次`hexo g`
* `hexo d`error 則輸入 `npm install hexo-deployer-git --save`，並在執行一次`hexo d`
13. 到GitHub repository

[:green_book: mac下搭建hexo+github](https://zhuanlan.zhihu.com/p/75438236)

[:green_book: Git+Hexo-架設個人靜態網站](https://hackmd.io/@king87515/Sy16ckymU#STEP4--%E5%AE%89%E8%A3%9DThemeNext)

---

## 